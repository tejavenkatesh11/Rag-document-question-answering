# Direct dependencies, pinned to the versions this project was built and
# tested against. Transitive dependencies are resolved by pip.

# --- Orchestration -------------------------------------------------------
langchain==1.4.0
langchain-classic==1.0.8          # EnsembleRetriever moved here in v1
langchain-community==0.4.2        # FAISS + BM25 + document loaders
langchain-text-splitters==1.1.2

# --- LLM -----------------------------------------------------------------
langchain-groq==1.1.3

# --- Embeddings ----------------------------------------------------------
# torch is CPU-only: install it from the PyTorch CPU index first, otherwise
# pip pulls the much larger CUDA build. See the README setup section.
torch==2.14.0+cpu
# Required even though this pipeline never processes images: transformers'
# lazy-import machinery reaches for its vision modules when anything walks
# the package (Streamlit's file watcher does), and without torchvision each
# attempt prints a full traceback - ~300 of them on startup.
torchvision==0.29.0+cpu
sentence-transformers==6.0.1
langchain-huggingface==1.2.2

# --- Vector store and retrieval ------------------------------------------
faiss-cpu==1.15.0
rank-bm25==0.2.2                  # sparse half of hybrid retrieval

# --- Document handling ---------------------------------------------------
pypdf==6.18.1

# --- Interface -----------------------------------------------------------
streamlit==1.63.0
python-dotenv==1.2.3

# --- Development ---------------------------------------------------------
pytest==9.1.1
reportlab==5.0.1                  # only for regenerating the sample PDF
